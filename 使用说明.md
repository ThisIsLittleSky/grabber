# 选课抢课 · 独立工具（Node.js + Playwright）

相对油猴脚本 `选课助手.user.js` 的独立版本：**虚拟浏览器登录 + 纯接口抢课**，可无人值守、可放服务器。

- 原理与接口分析见 [`docs/接口分析.md`](docs/接口分析.md)，整体方案见 [`docs/开发计划2.md`](docs/开发计划2.md)。
- **登录由你手动完成**（登录偶发验证码，工具不自动登录、不存账号密码）。
- 抢课循环走纯 HTTP 请求（复用登录会话），不依赖页面 UI。

---

## 目录结构

```
grabber/
  package.json          # 依赖：playwright
  config.json           # 目标课程 + 参数（含账号密码，敏感，勿提交）
  config.example.json   # 配置模板（复制为 config.json 后填写，可提交）
  grab.js               # 入口：登录 → 采集模板 → 抢课循环
  state.json            # 会话持久化（登录后自动生成，含 cookie，勿外传）
  templates.json        # 采集到的请求参数模板（自动生成）
  grab.log              # 运行日志（自动追加）
  .gitignore            # 忽略 node_modules / *.log / config.json / state.json / templates.json
  docs/
    接口分析.md          # 选课接口逆向分析
    开发计划.md          # 油猴脚本版方案
    开发计划2.md         # 本独立工具方案
  lib/
    match.js            # 目标解析 + 模糊匹配
    schedule.js         # 上课时间/周次解析 + 冲突判定
    conflict.js         # 冲突源定位 + 退选重选（只退可退选修课）
    params.js           # 参数序列化 + 模板拼装
    notify.js           # 日志/控制台/webhook/提示音
    self_test.js        # 单元测试（node lib/self_test.js）
```

## 前置准备

- 安装 Node.js（>= 16）。
- 在 `grabber` 目录安装依赖：`npm install`（首次会自动下载 Playwright 浏览器，需联网）。
- 首次使用：复制 `config.example.json` 为 `config.json`，再按需填写（`config.json` 不提交到 git）。

## 快速开始

1. **配置目标**：编辑 `config.json` 的 `targets`，**`kch` / `jxb` / `kw` 至少填一个**，命中任一即触发：

   | 字段 | 判定 | 示例 | 匹配方式 |
   |---|---|---|---|
   | `kch` | 纯数字 | `207786` | 与列表 `kch` 完全相等 |
   | `jxb` | 32 位十六进制 | `5706B5A0B5F80E52E0630B02FD0A792D` | 与列表 `jxb_id` 完全相等（不区分大小写） |
   | `kw` | 任意词 | `摄影` | 课程名 `kcmc` 或 教学班名 `jxbmc` 包含该词 |
   | `njdm_id` | 年级（可选） | `2025` | 只抢该年级的课，作用于所有目标；留空=不按年级过滤 |

   每个字段支持单个值、逗号（中英文均可）/换行分隔的多个值、或数组。示例：

   ```json
   "targets": {
     "kch": "207786,207787",
     "jxb": "5706B5A0B5F80E52E0630B02FD0A792D",
     "kw": "摄影",
     "njdm_id": "2025"
   }
   ```

   > `njdm_id` 即年级过滤（默认 `2025`）。列表课没带年级字段时自动**不过滤**、不会误挡（张家口学院的列表就不带 `njdm_id`）。

2. **先干跑验证**：`config.json` 里 `dryRun` 默认 `true`（只查询匹配、不提交）。
3. **启动**：在 `grabber` 目录运行 `node grab.js`。
4. **登录**：会弹出浏览器打开选课页。config 里配了 `login` 账号密码时会**自动填充登录表单**；若出现验证码，你在浏览器里补一下并点登录。脚本随后自动在后台抓取请求参数。
5. 日志出现"模板已就绪，保存会话"后自动进入抢课循环；命中且非干跑时会自动选课。
6. 确认匹配无误报后，把 `dryRun` 改为 `false` 重新运行，即可实跑。

## config.json 参数

| 键 | 默认 | 说明 |
|---|---|---|
| `targets` | `{kch:"",jxb:"",kw:"",njdm_id:"2025"}` | 目标课程：`kch`/`jxb`/`kw` 至少填一个；`njdm_id` 年级过滤可选 |
| `intervalSec` | `2.5` | 查询间隔秒数，勿低于 1.5（防限流/封禁） |
| `kklxdm` | `10` | 课程类别（10=通识选修），按需改 |
| `dryRun` | `true` | `true`=只监控不提交；`false`=实跑自动选课 |
| `headless` | `false` | 登录阶段是否无头（服务器上用 `true`，但登录仍需本地有头一次） |
| `loginTimeoutMin` | `5` | 等待登录就绪的最大分钟数 |
| `login` | `{username,password}` | 登录账号密码（选填）。配置后自动填充登录表单；有验证码仍需你手动补并点登录。留空则纯手动登录 |
| `notify.webhook` | `""` | 成功/会话过期推送 URL（ServerChan/PushPlus 类，POST `{title,desp}`） |
| `notify.beep` | `true` | 成功/失败时控制台提示音 |

> `state.json` / `templates.json` 为运行时生成，自动复用。换机器/换账号需删除后重新登录一次。

## 会话与无人值守

- 登录成功即把会话写入 `state.json`，**下次运行直接复用，无需再登**；会话过期会自动检测并提示重登。
- **服务器部署**：本地登录一次 → 把 `grabber` 整个目录（含 `state.json`、`templates.json`、`config.json`）拷到服务器 → `headless: true` + `dryRun: false` 运行 → 配好 `notify.webhook` 收通知。
- 服务器上会话过期：提示后需回本地重登生成新的 `state.json` 再传回。

## 冲突自动重选规则（沿用油猴版保守策略）

提交返回"冲突"时，工具查询已选课程，解析上课时间找与你目标课**时间重叠**的课：

- 重叠课是**可退的选修课**（`kklxdm=10` 且 `rwlx=2`）→ 自动退掉，重新提交目标课。
- 其余情况（必修/不可退/定位不到/退选失败）→ **一律只告警不动手**，冷却后重试。
- 一句话：**只退可退选修课，绝不误退必修课。** 退选不可逆，实跑前务必先干跑确认。

## 故障排查

| 现象 | 处理 |
|---|---|
| 卡在"请手动登录" | 确认浏览器里已完成登录并进入选课页；检查是否是选课开放时段 |
| 一直"等待模板" | 登录后页面没自动发请求 → 手动点一次页面"查询/展开"，或刷新页面 |
| `收到HTML响应` | 会话过期 → 删掉 `state.json` 重跑一次登录 |
| 查询返回 0 门课 | 检查 `kklxdm` 类别是否正确、是否过了选课时间窗口 |
| 日志「教学班详情返回 0 / 无可用教学班」 | 该课当前对你**不可选**（名额满/限选/批次原因），脚本已降频 60s 自动重试，名额放出会自动抢；可先在浏览器里对这门课点「选课」确认是否同样报错。若浏览器能展开而脚本报 0，多半是模板过旧 → 删掉 `templates.json`/`state.json` 重新登录采集一次 |
| 日志「[已选]…已在你的已选课表中」 | 目标已选上，脚本自动标记完成、不再重复抢 |
| 提交 flag=0 名额满 | 正常，冷却 30s 后自动重试 |

## 安全边界

- 不做验证码 OCR：登录出现验证码时由你手动补（账号密码自动填充，不识别图片）。
- `config.json` 的 `login` 明文存账号密码，`state.json` 含登录会话 cookie——**两者均属敏感文件，勿外传/勿进 git**。仓库 `.gitignore` 已默认忽略 `config.json` / `state.json` / `templates.json` / `*.log` / `node_modules`，上传前无需手动删除。
- 冲突自动退选严格限定"可退选修课"，其他冲突只告警。
- 频率勿开太高，默认 2.5s 已够用。
